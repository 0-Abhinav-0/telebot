{
  "name": "whatsapp-file-retrieval-system",
  "version": "1.0.0",
  "description": "A backend system for uploading and retrieving files via WhatsApp using Twilio API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup": "npm install && mkdir -p uploads",
    "tunnel": "ngrok http 3000",
    "test": "curl -X GET http://localhost:3000/health"
  },
  "keywords": [
    "whatsapp",
    "twilio",
    "file-upload",
    "nodejs",
    "express",
    "multer",
    "ngrok"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.10.0",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "twilio": "^4.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
